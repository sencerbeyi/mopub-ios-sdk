import XCTest
@testable import mopub_ios_sdk_lastgear

final class mopub_ios_sdk_lastgearTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(mopub_ios_sdk_lastgear().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
